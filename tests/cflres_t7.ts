import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("cflres 7",()=>{it("c7",()=>{const r=simnet.callReadOnlyFn("timetablechain","get-last-token-id",[],simnet.deployer);expect(r.result).toBeDefined();});});
