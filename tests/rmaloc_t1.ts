import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("rmaloc 1",()=>{it("c1",()=>{const r=simnet.callReadOnlyFn("timetablechain","get-last-token-id",[],simnet.deployer);expect(r.result).toBeDefined();});});
