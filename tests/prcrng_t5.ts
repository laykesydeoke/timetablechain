import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("prcrng 5",()=>{it("c5",()=>{const r=simnet.callReadOnlyFn("timetablechain","get-last-token-id",[],simnet.deployer);expect(r.result).toBeDefined();});});
