import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("apiver integration 1",()=>{it("full flow",()=>{const r=simnet.callReadOnlyFn("timetablechain","get-last-token-id",[],simnet.deployer);expect(r.result).toBeDefined();});});
