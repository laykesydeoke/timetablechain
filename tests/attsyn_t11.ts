import{describe,it,expect}from"vitest";import{simnet}from"./setup";describe("attsyn 11",()=>{it("c11",()=>{const r=simnet.callReadOnlyFn("timetablechain","get-last-token-id",[],simnet.deployer);expect(r.result).toBeDefined();});});
